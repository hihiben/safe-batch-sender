{
  "name": "safe-batch-sender",
  "version": "1.0.0",
  "description": "A static Safe App that proposes a batch of native-token transfers from a payload encoded in the link.",
  "private": true,
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hihiben/safe-batch-sender.git"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@safe-global/safe-apps-sdk": "^9.1.0",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "happy-dom": "^20.12.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
